# Wojak Gesture Controller - Dependencies
# Install with: pip install -r requirements.txt
#
# NOTE: mediapipe 0.10.14 is pinned because later builds (>=0.10.21+) on some
# platforms removed the legacy mp.solutions namespace. If you encounter
# "module 'mediapipe' has no attribute 'solutions'", ensure you are on this
# version: pip install mediapipe==0.10.14

opencv-python>=4.8.0,<5.0.0
mediapipe==0.10.14
numpy>=1.24.0,<2.0.0
